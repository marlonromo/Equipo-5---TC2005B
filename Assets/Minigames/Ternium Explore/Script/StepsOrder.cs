using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StepsOrder : MonoBehaviour
{
  // public GameObject mina;
  // public GameObject pellets;
  // public GameObject pellets2;
  // public GameObject aceracion;
  // public GameObject laminacion;
  // public GameObject camion;
    // Start is called before the first frame update
    void Start()
    {
      Time.timeScale = 1;
    }

    // Update is called once per frame
    void Update()
    {
      // if(mina.active == false && pellets.active == false){
      //   pellets.SetActive(true);
      // }
      // if(mina.active == false && pellets.active == false && pellets2.active == false){
      //   pellets2.SetActive(true);
      // }
    }
}
